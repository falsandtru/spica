import { global } from './global';

describe('Unit: lib/global', () => {
  describe('global', () => {
    it('', () => {
      assert(global);
      assert(global.global === global);
    });

  });

});
